{
  "name": "sample-document-site",
  "private": true,
  "type": "module",
  "scripts": {
    "blume:prepare": "node scripts/prepare-content.mjs",
    "blume:build": "npm run blume:prepare && blume build --strict",
    "blume:dev": "blume dev --host 0.0.0.0",
    "blume:validate-links": "node scripts/validate-links.mjs",
    "blume:test:mcp-search": "node scripts/test-mcp-search.mjs"
  },
  "dependencies": {
    "blume": "1.5.3"
  }
}
